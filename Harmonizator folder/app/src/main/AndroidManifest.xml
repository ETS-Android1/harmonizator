<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.helloworld">

    <application
        android:allowBackup="true"
        android:icon="@drawable/harmonizator"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloWorld"
        android:debuggable="false"
        tools:ignore="HardcodedDebugMode">
        <activity android:name="com.example.harmonizator.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".IntervalyActivity"></activity>
        <activity android:name=".StupneActivity"></activity>
        <activity android:name=".AkordyActivity"></activity>
        <activity android:name=".DurActivity"></activity>
        <activity android:name=".MolActivity"></activity>
    </application>

</manifest>